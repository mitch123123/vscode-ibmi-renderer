## Summary

Briefly describe what this PR changes and why.

## Checklist

- [ ] `npm run compile` passes locally
- [ ] `npm test` passes (new model behavior has Vitest coverage)
- [ ] `CHANGELOG.md` updated under **Unreleased** (if user-visible)
- [ ] Bump `package.json` `"version"` if this merge should publish to the Marketplace
- [ ] Rebuilt and committed `webui/main.js` if `webui/src/` changed
- [ ] Opened / linked an issue for large DDS contract changes

## Test plan

- [ ]
